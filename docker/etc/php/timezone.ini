; Force PHP timezone in containers (CLI + php-fpm) so UI timestamps match the VM timezone.
; This is separate from AtoM's `default_timezone` setting (apps/qubit/config/settings.yml).
date.timezone = Europe/Zurich

